## Автозагрузка топологии и политик при старте брокера (DLX, отстойник и пр.).
## Отдельный drop-in, чтобы не трогать 10-defaults.conf из образа
## (там loopback_users.guest = false — доступ guest по сети).
load_definitions = /etc/rabbitmq/definitions.json
