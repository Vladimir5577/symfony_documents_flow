================================================================================
SPA API — HR Analytics
================================================================================

Эндпоинт для получения отчётов «Отдел кадров» в виде дерева метрик по неделям.
Никакой агрегации/расчётов на лету — отдаются ровно те значения, которые
оператор ввёл в подтверждённых отчётах, в иерархии, заданной составом
версии доски (`analytics_board_version_metrics.parent_id`).

Контракт и формат — общий с финансовой аналитикой и аналитикой обращений
граждан (см. Readme_financial_analytics.txt, Readme_citizen_appeal.txt);
отличается только категория метрик (`hr`) и состав дерева.

--------------------------------------------------------------------------------
1. ЗАПРОС
--------------------------------------------------------------------------------

  Method : GET
  URL    : /spa/api/analytics/hr/reports

Query-параметры:

  org_id  (int, опционально, по умолчанию 0)
            ID родительской организации.
            0 = все видимые родительские организации (объединение их
                самих и всех дочерних);
            N = конкретная организация N + все её дочерние.

  from    (string YYYY-MM-DD, опционально)
            Нижняя граница периода: возвращаются только недели,
            у которых analytics_periods.start_date >= from.
            Невалидное значение (не подходящее под YYYY-MM-DD)
            игнорируется без ошибки.

  to      (string YYYY-MM-DD, опционально)
            Верхняя граница периода: analytics_periods.end_date <= to.
            Невалидное значение игнорируется.

  limit   (int, опционально, по умолчанию 12, максимум 100)
            Сколько недель вернуть. limit <= 0 → 12. limit > 100 → 100.
            Пагинация — по неделям, а не по строкам метрик.

  offset  (int, опционально, по умолчанию 0)
            Сдвиг по неделям. offset < 0 → 0.

Авторизация: JWT в заголовке Authorization (как у всех /spa/api/* маршрутов).
См. dev_docks/spa_api/Readme_spa_auth.txt.

Примеры URL:

  // последние 12 недель организации 20
  /spa/api/analytics/hr/reports?org_id=20

  // следующая страница (предыдущие 12 недель)
  /spa/api/analytics/hr/reports?org_id=20&offset=12

  // апрель–май 2026, все недели в диапазоне
  /spa/api/analytics/hr/reports?org_id=20&from=2026-04-01&to=2026-05-31&limit=100

  // только одна последняя неделя
  /spa/api/analytics/hr/reports?org_id=20&limit=1

curl:

  curl -H "Authorization: Bearer <token>" \
       "https://example.local/spa/api/analytics/hr/reports?org_id=20&from=2026-04-01"

fetch:

  const params = new URLSearchParams({
    org_id: '20',
    from:   '2026-04-01',
    to:     '2026-05-31',
    limit:  '12',
    offset: '0',
  });
  const res = await fetch(
    `/spa/api/analytics/hr/reports?${params}`,
    { headers: { Authorization: "Bearer " + jwt } }
  );
  const data = await res.json();

--------------------------------------------------------------------------------
2. ФОРМАТ ОТВЕТА
--------------------------------------------------------------------------------

HTTP 200, application/json.

Корневой объект:

{
  "weeks": [
    {
      "startDate": "YYYY-MM-DD",   // analytics_periods.start_date
      "endDate":   "YYYY-MM-DD",   // analytics_periods.end_date
      "reports":   MetricNode[]    // дерево метрик за этот период
    },
    ...
  ]
}

Имя поля `weeks` отражает текущую конфигурацию HR-доски (weekly):
startDate — понедельник, endDate — воскресенье ISO-недели. Если в будущем
доска будет переведена на daily/monthly, формат ответа не меняется:
одна запись = один analytics_periods, поля те же.

Недели в ответе отсортированы по startDate по возрастанию (от старой к
свежей). Пагинация (limit/offset) работает «от свежих к старым»: limit=12,
offset=0 — последние 12 недель; offset=12 — следующая страница с предыдущими
12 неделями. Внутри одной страницы порядок всегда ASC.
В выборку попадают только отчёты со статусом `confirmed`.

Узел дерева MetricNode:

{
  "metric_key":  string,         // business_key метрики (hr_*)
  "name":        string,         // отображаемое имя метрики
  "unit":        string|null,    // единица измерения ("чел.", "%")
  "valueNumber": number|null,    // числовое значение; null = не заполнено
  "valueJSON":   any|null,       // структурированные пояснения; null если пусто
  "children":    MetricNode[]    // подметрики; [] если нет детей
}

Правила формирования:

- В каждой неделе возвращается **полный** набор метрик из версии доски,
  привязанной к отчёту, — независимо от того, заполнено значение или нет.
  Метрики без заполненного значения отдают `valueNumber: null` и
  `valueJSON: null`. Это позволяет фронту нарисовать полный каркас формы
  и проставить прочерк там, где значения нет.
- Порядок узлов на одном уровне — по `analytics_board_version_metrics.position`
  по возрастанию.
- Иерархия строится по `parent_id`: корнями являются метрики с `parent_id = NULL`.
- `valueJSON` обычно null для HR-метрик; используется только если оператор
  добавил расшифровку (структурированный комментарий).
- Категория метрик жёстко зафиксирована в эндпоинте как `hr`.

--------------------------------------------------------------------------------
3. СТРУКТУРА ДЕРЕВА
--------------------------------------------------------------------------------

Текущая версия HR-доски содержит 45 метрик
(`analytics_metrics.category = 'hr'`) в пяти корневых узлах — по одному
на каждый «вид» показателя, с одинаковой раскладкой по филиалам:

  Штатная численность — Итого              (hr_staff_planned_total)
    ├─ Головная организация                (hr_staff_planned_head_office)
    ├─ Донецкий филиал                     (hr_staff_planned_donetsk)
    ├─ Мариупольский филиал                (hr_staff_planned_mariupol)
    ├─ Макеевский филиал                   (hr_staff_planned_makeevka)
    ├─ Шахтёрский филиал                   (hr_staff_planned_shakhtersk)
    ├─ Горловский филиал                   (hr_staff_planned_gorlovka)
    ├─ Енакиевский филиал                  (hr_staff_planned_enakievo)
    └─ Амвросиевский филиал                (hr_staff_planned_amvrosievka)

  Фактическая численность — Итого          (hr_staff_actual_total)
    └─ те же 8 филиалов                    (hr_staff_actual_*)

  Принято — Итого                          (hr_hired_total)
    └─ те же 8 филиалов                    (hr_hired_*)

  Уволено — Итого                          (hr_fired_total)
    └─ те же 8 филиалов                    (hr_fired_*)

  Укомплектованность штата — Итого         (hr_staffing_level_total)
    └─ те же 8 филиалов                    (hr_staffing_level_*)

Единицы измерения:
  Штатная / Фактическая / Принято / Уволено  →  "чел."
  Укомплектованность штата                    →  "%"

Состав дерева определяется версией доски, к которой привязан отчёт.
Любые правки в админке (`/analytics/admin/board/...`) сразу отражаются
на ответе без перевыкатки клиента.

--------------------------------------------------------------------------------
4. ПРИМЕР ОТВЕТА (сокращённый)
--------------------------------------------------------------------------------

GET /spa/api/analytics/hr/reports?org_id=20

{
  "weeks": [
    {
      "startDate": "2026-05-18",
      "endDate":   "2026-05-24",
      "reports": [
        {
          "metric_key": "hr_staff_planned_total",
          "name":       "Штатная численность — Итого",
          "unit":       "чел.",
          "valueNumber": 4481,
          "valueJSON":   null,
          "children": [
            {
              "metric_key": "hr_staff_planned_head_office",
              "name":       "Штатная численность — Головная организация",
              "unit":       "чел.",
              "valueNumber": 955,
              "valueJSON":   null,
              "children":    []
            },
            {
              "metric_key": "hr_staff_planned_donetsk",
              "name":       "Штатная численность — Донецкий филиал",
              "unit":       "чел.",
              "valueNumber": 948,
              "valueJSON":   null,
              "children":    []
            }
            // ... mariupol, makeevka, shakhtersk, gorlovka, enakievo, amvrosievka
          ]
        },
        {
          "metric_key": "hr_staff_actual_total",
          "name":       "Фактическая численность — Итого",
          "unit":       "чел.",
          "valueNumber": 1850,
          "valueJSON":   null,
          "children": [ /* 8 филиалов */ ]
        },
        {
          "metric_key": "hr_hired_total",
          "name":       "Принято — Итого",
          "unit":       "чел.",
          "valueNumber": 22,
          "valueJSON":   null,
          "children": [ /* 8 филиалов */ ]
        },
        {
          "metric_key": "hr_fired_total",
          "name":       "Уволено — Итого",
          "unit":       "чел.",
          "valueNumber": 12,
          "valueJSON":   null,
          "children": [ /* 8 филиалов */ ]
        },
        {
          "metric_key": "hr_staffing_level_total",
          "name":       "Укомплектованность штата — Итого",
          "unit":       "%",
          "valueNumber": 41.3,
          "valueJSON":   null,
          "children": [ /* 8 филиалов */ ]
        }
      ]
    },
    {
      "startDate": "2026-05-11",
      "endDate":   "2026-05-17",
      "reports":   [ /* такая же структура с другими значениями */ ]
    }
    // ... остальные недели
  ]
}

--------------------------------------------------------------------------------
5. ПРИМЕР ПУСТОГО ОТВЕТА
--------------------------------------------------------------------------------

Если по запрошенным организациям нет ни одного `confirmed`-отчёта —
HTTP 200 с пустым массивом недель:

{
  "weeks": []
}

--------------------------------------------------------------------------------
6. КРАЕВЫЕ СЛУЧАИ
--------------------------------------------------------------------------------

- В выборке только подтверждённые отчёты (`status = 'confirmed'`).
  Черновики (`draft`) **не отдаются** — они не должны влиять на аналитику.

- Метрики, не заполненные в отчёте, всё равно присутствуют в дереве
  с `valueNumber: null`. Фронт сам решает: показывать прочерк, скрывать
  узел или выделять как «незаполнено».

- Иерархия живёт только в визуальном представлении формы и таблиц.
  Никаких автосумм `parent = Σ children` нет: значения «Итого» оператор
  вводит вручную, и они могут расходиться с суммой детей.

- Сортировка узлов на одном уровне — строго по `position`. Изменение
  состава версии доски (`/analytics/admin/board/...`) меняет порядок
  отдачи без переразвёртывания клиента.

- Состав филиалов и сам факт наличия раздела определяется составом
  версии доски, привязанной к отчёту, а не справочником
  `analytics_metrics`. Метрика в справочнике без привязки к версии
  в ответе не появится.

- Эндпоинт возвращает **только** HR-метрики (category = 'hr').
  Для других категорий (finance, citizen_appeal, tko, ...)
  предусмотрены отдельные эндпоинты.

- При указании одновременно from/to и limit/offset фильтр диапазона
  применяется первым, затем уже работает пагинация по отфильтрованным
  неделям.

- Невалидные значения query-параметров (например, from=invalid) не
  возвращают ошибку — они просто игнорируются, чтобы клиент мог
  безопасно строить URL.

- KPI (численность, текучесть, и т.п.) и кросс-таблица сравнения по
  организациям этим эндпоинтом не отдаются — нужные показатели
  считаются на стороне фронта из значений «… — Итого» и/или из значений
  по конкретным филиалам. Сравнение по орг-ям делается несколькими
  запросами с разным `org_id`.

--------------------------------------------------------------------------------
7. СВЯЗАННЫЕ ФАЙЛЫ
--------------------------------------------------------------------------------

  Контроллер : src/Controller/SpaApi/Analytics/HrAnalyticsController.php
  Сервис     : src/Service/Analytics/HrReportTreeService.php
  Репозиторий: src/Repository/Analytics/AnalyticsReportValueRepository.php
               (метод findReportsWithMetricTree — общий с финансовой
               аналитикой и обращениями граждан)
  Финансы    : dev_docks/spa_api/Readme_financial_analytics.txt
  Обращения  : dev_docks/spa_api/Readme_citizen_appeal.txt
  Auth       : dev_docks/spa_api/Readme_spa_auth.txt
