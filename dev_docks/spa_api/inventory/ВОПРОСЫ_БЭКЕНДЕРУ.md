# Вопросы бэкендеру по проду инвентаризации

Обновлено 28.07.2026, после починки. Контекст: модуль работает на `http://31.133.49.124:8075`.
В репозитории **нет ни одной миграции**, создающей таблицы `inventory_*` — все 17 файлов
в `migrations/` к модулю отношения не имеют, при этом на проде роуты отвечают.
Значит схему накатывали как-то иначе, и это главный оставшийся вопрос.

Отвечать можно коротко, но по пунктам.

---

## 0. Что уже починено — нужен деплой и короткая проверка

Причина 500 найдена и устранена **дважды независимо**: вами (`ef38ec8`) и нами — пришли
к одной и той же строке. Падал не раздел устройств, как показывал тост, а
`GET /spa/api/inventory/stock/my`: в `StockRepository::findByHolderUser()` выбиралось
`IDENTITY(s.nomenclature)`, а группировка шла по `n.id`. Экран «моё имущество» из-за этого
всегда показывал пустое состояние — не потому, что имущества нет, а потому что запрос падал.
В ветке оставлена ваша редакция.

Сверх этого в `459077f`:

| Что | Зачем |
|---|---|
| `SpaApiExceptionListener` | доменные ошибки под `/spa/api/` приводятся к `{error: <код>}` |
| `device_id` в строках документа | поле читается и отдаётся; раньше не записывалось нигде |
| `app:inventory:verify-stock` | проверка инварианта `stock = Σ movement` |

**Проверить после деплоя (2 минуты):**

0.1. Открыть `/my-property` — 500 должен уйти.

0.2. Попробовать списать больше, чем есть на остатке. Ожидается русское
     «Не хватает остатка. Проверьте количество и склад-источник.»
     Если видите пустую ошибку или «Внутренняя ошибка сервера» — слушатель не подхватился,
     напишите, будем разбираться.

0.3. `php bin/console app:inventory:verify-stock` — на пустой базе ожидается «Расхождений нет».

⚠ Всё это проверено только `php -l`: на машине, где правилось, нет `vendor/`,
поэтому `phpunit`, `lint:container` и `doctrine:schema:validate` не запускались.
Первый настоящий прогон — ваш.

---

## 1. Откуда взялась схема БД (главный вопрос)

1.1. Как создавались 22 таблицы `inventory_*` на проде: применяли `invent/ddl_spec.sql`
     напрямую (psql/DBeaver) или запускали `doctrine:migrations:migrate` с файлом миграции?

1.2. Если был файл миграции — где он? В ветке `inventory` его нет. Другая ветка, не закоммичен,
     лежит только на сервере?

1.3. Что показывает на проде:
```sql
SELECT version FROM doctrine_migration_versions ORDER BY executed_at DESC LIMIT 5;
```
Это критично: если Doctrine не знает про инвентарные таблицы, следующий `migrate`
на другом стенде их не создаст, и среды разъедутся.

1.4. Сколько таблиц реально есть (ожидается 22):
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public' AND table_name LIKE 'inventory%'
ORDER BY 1;
```

---

## 2. Целы ли защиты БД (без них ledger-инварианты держатся только на коде)

Прислать вывод целиком:

```sql
-- 2.1. CHECK-констрейнты: их 8+ по спеке, последняя защита от отрицательного остатка
SELECT conrelid::regclass AS tbl, conname, pg_get_constraintdef(oid)
FROM pg_constraint
WHERE contype = 'c' AND conrelid::regclass::text LIKE 'inventory%'
ORDER BY 1, 2;

-- 2.2. Главный арбитр UPSERT остатков. ОБЯЗАН быть UNIQUE NULLS NOT DISTINCT.
SELECT conname, pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conname = 'uniq_inv_stock_key';

-- 2.3. Расширение для поиска по номенклатуре
SELECT extname FROM pg_extension WHERE extname = 'pg_trgm';

-- 2.4. Тип ip_address: по спеке INET
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'inventory_device' AND column_name = 'ip_address';
```

**Почему это важно.** Код в `StockPostingService` делает
`ON CONFLICT ON CONSTRAINT uniq_inv_stock_key`. Если констрейнта с таким именем нет
(например, схему накатывали `doctrine:schema:update`, который его не создаёт), то **каждое
проведение документа падает с SQLSTATE 42704**. Если он есть, но без `NULLS NOT DISTINCT` —
хуже: ошибки не будет, а остатки начнут плодить дубли строк, и разойдётся это не сразу,
а через несколько документов.

---

## 3. Окружение

3.1. `APP_ENV` на проде — `prod` или `dev`?

3.2. Заданы ли переменные и не пустые ли:
- `INVENTORY_CREDENTIALS_KEY` (для Ф4 пустым быть не может — доступы устройств не сохранятся)
- `PRIVATE_UPLOAD_DIR_INVENTORY` (без неё контейнер не соберётся)

3.3. Какие коммиты реально задеплоены — бэкенд и фронт отдельно?
```
git -C <бэкенд> rev-parse --short HEAD && git -C <бэкенд> branch --show-current
git -C <фронт>  rev-parse --short HEAD && git -C <фронт>  branch --show-current
```
На момент написания в ветках: бэкенд `459077f`, фронт `bea0bc3a`.

3.4. Выполнялся ли `php bin/console app:roles:sync` после деплоя? Без него новые инвентарные
     права не появятся в реестре и роли не выдать через админку `/roles`.

---

## 4. Ф4: устройства в строках документов — осталось доделать фронт

Поле `device_id` теперь принимается и возвращается бэкендом, но **UI его не отправляет**.
Пока это так, сценарии «расход картриджа в принтер» и «перемещение устройства сотруднику»
через интерфейс недоступны, а размещение устройства не вычисляется — оно выводится
из приходящих движений с этим `device_id`, а их пока не появится.

4.1. Кто доделывает фронт — вы или мы? Нужен селектор карточки устройства в строке документа
     для типов `transfer`, `writeoff`, `consumption`.

---

## 5. Версия конверта шифрования доступов

Переход с libsodium на OpenSSL AES-256-GCM принят — `ext-sodium` не требуется, вопрос не в этом.

Вопрос в версии. Формат конверта изменился:

| | было (secretbox) | стало (GCM) |
|---|---|---|
| раскладка | `nonce(24) ‖ cipher` | `nonce(12) ‖ tag(16) ‖ cipher` |
| префикс | `v1:` | **`v1:`** |

`CURRENT_KEY_VERSION` остался `1`, хотя сам класс документирует механизм: «Ротация: новый ключ =
новая версия; decrypt выбирает ключ по префиксу версии». Сейчас `v1:` означает два несовместимых
формата, и старая запись при расшифровке даст «wrong key or tampered data» — то есть будет
выглядеть как взлом или потеря ключа, а не как смена формата.

5.1. Сохранялись ли хоть раз доступы устройств на проде до этой правки?
```sql
SELECT COUNT(*), MIN(created_at), MAX(created_at) FROM inventory_device_credential;
SELECT DISTINCT left(secret_cipher, 3) FROM inventory_device_credential;
```
Если таблица пуста — вопрос закрыт, делать ничего не надо.

5.2. Если записи есть — расшифровать их уже нечем, и правильнее не чинить задним числом,
     а поднять `CURRENT_KEY_VERSION` до `2` и завести доступы заново. Тогда следующая смена
     формата не создаст той же двусмысленности.

5.3. Мелочь оттуда же: `sodium_memzero($plain)` заменён на `$plain = str_repeat("\0", strlen($plain))`.
     Это не затирает исходную память, а только переприсваивает переменную — прежняя строка
     остаётся в куче до сборки мусора. Не критично, но и не эквивалент: если затирающий аналог
     нужен, в OpenSSL-ветке его нет, и честнее убрать строку, чем считать её защитой.

---

## 6. Про историю ветки

Ветку `inventory` за день дважды перезаписали `push --force`. Во второй раз стёрся коммит
с документацией по ревью — его пришлось восстанавливать `cherry-pick`. Ничего непоправимого
не случилось, но при параллельной работе так теряются чужие правки, причём молча.

6.1. Можно ли перейти на обычный `push` и не переписывать историю? Если нужен сквош —
     лучше делать его в отдельной ветке и вливать через PR.

---

## 7. Прочее

7.1. Пользовались ли уже импортом из 1С и сверкой на реальных данных? Сколько строк, что вышло?

7.2. Есть ли жалобы пользователей на непонятные ошибки без текста? (До правки `459077f`
     доменные ошибки модуля до интерфейса не доходили вовсе — это объясняло бы жалобы.)

7.3. Делались ли правки прямо на сервере, мимо git? Если да — какие файлы.
